# qgc
An adaptive Lua GC scheduler from Quick engine


✨ **Core Features**  
- **Reduces runtime memory usage by 30%** 
- **CPU consumption less than 1%**

# usages:
```lua 

-- Call per second (use with qxtable.gc() or qstable.gc() to release memory)
qgc.tick()

```

